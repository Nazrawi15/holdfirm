import { type YoClient } from '@yo-protocol/core';
import { type ReactNode } from 'react';
interface YieldContextValue {
    ready: boolean;
    partnerId?: number;
    defaultSlippageBps: number;
    onError?: (error: Error) => void;
}
export interface YieldProviderProps {
    children: ReactNode;
    partnerId?: number;
    defaultSlippageBps?: number;
    onError?: (error: Error) => void;
}
/**
 * YieldProvider marks the boundary where @yo-protocol/react hooks can be used.
 * Must be placed inside WagmiProvider and after any SSR guards.
 */
export declare function YieldProvider({ children, partnerId, defaultSlippageBps, onError, }: YieldProviderProps): import("react/jsx-runtime").JSX.Element;
/**
 * Hook to access YieldProvider config (partnerId, slippage, onError).
 */
export declare function useYieldConfig(): YieldContextValue;
/**
 * Hook that creates and returns the YoClient.
 * Returns null when providers aren't ready.
 */
export declare function useYoClient(): YoClient | null;
export {};
//# sourceMappingURL=context.d.ts.map